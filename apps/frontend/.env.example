# API proxy target (used by Next.js rewrites in dev mode)
BACKEND_URL=http://localhost:4000

# Runtime config (not build-time - used via window origin)
NEXT_PUBLIC_APP_NAME=IPL Betting
NEXT_PUBLIC_APP_URL=http://localhost:3000
